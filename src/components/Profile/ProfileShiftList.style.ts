import { styled } from '@mui/system';

export const ProfileShiftListContainer = styled('div')(({ theme }) => ({
  display: 'flex',
  height: '100%',
  maxWidth: theme.breakpoints.values.md,
  width: '100%',
  margin: '0 auto',
}));
